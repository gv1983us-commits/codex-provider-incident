# Astra continuation failure · 10 September 2026

[Home](../README.md) · [Exact receipt and provenance](../evidence/astra-20260910T141745876Z.json) · [Recovery task](https://github.com/gv1983us-commits/codex-provider-incident/issues/1)

**Status: the owner reports that later Astra work failed after approximately ten further tool calls. The supplied receipt confirms a provider-layer error at 14:17:45.876 UTC, classified by Hermes as `unknown`. No sustained recovery is demonstrated.**

## Exact owner-supplied receipt

```text
── Hermes error details ──
time: 2026-09-10T14:17:45.876Z
layer: provider
code: unknown
retryable: true
provider: openai-codex
model: gpt-6-astra-900k
error: An error occurred while processing your request. You can retry your request, or contact us through our help center at help.openai.com if the error persists. Please include the request ID 2a438cad-4c97-43d4-abc8-77ce4e4ac03d in your message.
```

The receipt time is **17:17:45.876 Moscow (UTC+3)**. The identifier is explicitly called a request ID in the error message; no response headers have been supplied.

## What changes

| Evidence | Result | Scope |
| --- | --- | --- |
| 12:06:26.863 UTC, earlier GPT-5.5 receipt | `overloaded` | Exact owner-supplied error details |
| 12:18:50.270 UTC, Astra Medium final answer | Five paired tool calls/results and a final answer | Audited 29-message export, ending at 12:19 UTC |
| 14:17:45.876 UTC, new Astra receipt | `unknown`, `retryable: true`, request ID supplied | Separate owner-supplied error details, after that export |

The earlier successful turn remains a real scoped success. The later failure removes the basis for treating Astra as a demonstrated sustained workaround. The owner's approximately ten additional tool calls are a separate observation; their exact count, results and the failed call's role have not been audited. The interval between the two timestamps is not a measurement of continuous work or uptime.

The new record must not be counted as another confirmed overload. `unknown` is the classification displayed by Hermes; the receipt does not expose the raw upstream error object or explain the provider's internal cause. A common cause with the earlier overloads remains possible, but is not established by this text.

## Focused follow-up

Preserve the last completed tool result and pending model step. Match this timestamp and request ID to a small relevant log excerpt containing the actual call role, attempts, effective request settings and HTTP status if logged. The receipt names the Hermes model `gpt-6-astra-900k`; it does not capture the wire model or confirm that the earlier Medium/normal settings remained unchanged. `retryable: true` does not record a successful retry.

The new receipt remains separate from the original audit counters and the 29-message export. No additional live test, runtime patch, or provider-side diagnosis is claimed.

## По-русски

**Astra снова остановилась:** в 17:17:45.876 по Москве Hermes показал `provider / unknown` и request ID. Короткий успешный проход Medium сохранили как факт; устойчивый обход не подтверждён. По словам владельца, до отказа прошло ещё примерно десять вызовов инструментов. Точная квитанция не содержит HTTP-статуса, роли отказавшего вызова и актуального reasoning — эти поля не достраиваем из прежнего теста.
