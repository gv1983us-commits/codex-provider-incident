# Fresh-session evidence · 10 September 2026

[Home](../README.md) · [Reviewed data](../evidence/session-20260910-reviewed.json) · [Recovery task](https://github.com/gv1983us-commits/codex-provider-incident/issues/1)

**Finding:** the same test session progressed from an unsuccessful GPT-5.5 attempt and owner-reported Sol failures to a successful **Astra 900k / Medium text response and five-tool analysis turn**. This is evidence of intermittent availability. Sustained Jarvis work and automatic recovery from provider exceptions remain unverified.

**Later evidence, outside this export:** the [14:17:45.876 UTC Astra receipt](ASTRA-2026-09-10.md) records a provider error classified as `unknown`, with a request ID. The owner reports approximately ten further tool calls before it. This does not alter the audited short success below; sustained recovery is still not demonstrated. Current effort/session identity are not supplied by that receipt.

## Source and method

- Owner-supplied file: `gpt-5.5-ultra-hermes-20260910.json`.
- **98,545 bytes; 29 message records**; exported at **2026-09-10T12:19:03.621Z**.
- SHA-256: `432f0978229e52f84beb4550ac4810ca6fcc3761f2df4cec050586eaab3111c2`.
- Session: `20260910_150157_423354`.
- Roles: **9 user, 11 assistant, 9 tool**. Three model-change notices are stored with the user role; they are not three additional human instructions.
- Nine paired tool calls/results: **four in the initial phase, five in the Astra phase**.

The reviewed extract preserves all 29 message IDs, timestamps, tool pairings and concise event descriptions. Descriptions are paraphrases; full private skill instructions, reasoning payloads, local paths, unrelated DB details and full configuration are omitted. Timing differences were recomputed from the export, rather than accepted from the assistant's self-report.

## Sequence

All times below are **Moscow / UTC+3**.

| Time | Source IDs | Observed event |
| --- | --- | --- |
| 15:01:58.030 | 20346 | Owner starts a GPT-5.5 Ultra test |
| 15:02:24.785–15:04:36.872 | 20347–20353 | One skill read and three terminal calls return results; subsequent model tool calls occur |
| 15:04:42.862 | 20354–20355 | Hidden `[response interrupted]` marker and owner follow-up share a timestamp |
| 15:06:26.863 | Separate Copy details receipt | `openai-codex / gpt-5.5 / overloaded`, `retryable: true` |
| 15:14:52.493–15:15:15.069 | 20356–20357 | Switch notice to Sol; owner reports previous GPT-5.5 failure and tests Sol |
| 15:15:40.035–15:15:55.170 | 20358–20359 | Switch notice to Sol 900k; owner says previous attempts failed |
| 15:17:04.257–15:17:19.293 | 20360–20362 | Switch to Astra 900k; owner requests Medium; text answer ends with `stop` |
| 15:17:58.943–15:18:50.270 | 20363–20374 | Five tool calls, five results, then a final assistant answer with `stop` |

The separate GPT-5.5 receipt is [already recorded here](https://github.com/gv1983us-commits/codex-provider-incident/issues/1#issuecomment-5618436481). It is not a message in the new export. Do not equate its timestamp with the earlier hidden interruption marker or add a second overload solely because that marker exists.

The Sol test prompts have no intervening completed assistant responses in this export. The owner's failure reports are retained, but the exact Sol error codes and retry counts are absent.

## Astra's tool cycle is demonstrated

The successful phase contains:
1. `tool_describe` for session search.
2. `session_search` through the `tool_call` wrapper.
3. A read-only SQLite schema query.
4. A read-only query for this session's configuration and messages.
5. A read-only timestamp calculation.
6. A final assistant answer, `finish_reason=stop`.

Source pairs are **20364→20365, 20366→20367, 20368→20369, 20370→20371, 20372→20373**, followed by final **20374**. This is stronger evidence than a plain text response or a spinner. It does not prove long-duration stability or an automatic retry/resume repair: the owner manually changed models between phases.

## Configuration actually captured

A terminal result at **20371** contains a read-only session DB query. Its selected fields are:

```json
{
  "model": "gpt-6-astra-900k",
  "provider": "openai-codex",
  "reasoning_config": {"enabled": true, "effort": "medium"},
  "base_url": "https://chatgpt.com/backend-api/codex",
  "api_mode": "codex_responses",
  "service_tier": "normal"
}
```

This confirms **Medium in the saved Astra-phase configuration**, beyond the owner's request. It does not retroactively establish every GPT-5.5 parameter or capture the final wire request. `service_tier: normal` is a stored configuration value; it is not proof of subscription priority, account downgrade, or a particular server pool.

The session title still refers to GPT-5.5 while the stored current model is Astra. The export's top-level `session` field is `null`; the useful configuration comes from the retained terminal result. The exact installed full commit and customization delta remain unpinned.

## Measured intervals

| Interval | Seconds |
| --- | ---: |
| Initial prompt → first tool call | 26.755 |
| Initial skill result → next model tool call | 37.680 |
| `hermes doctor` call → returned result | 32.497 |
| Doctor result → next model tool calls | 57.429 |
| Astra text-test prompt → answer | 6.357 |
| Astra analysis prompt → first tool call | 3.689 |
| Astra analysis prompt → final answer | 51.327 |

These are **message timestamp intervals**. They combine runtime scheduling, tools, model work and delivery; they are not pure model compute or streaming throughput. In particular, the doctor operation itself accounts for about 32.5 seconds. Different models, effort, prompts and wall-clock conditions prevent attributing the speed difference to Medium alone.

## Other useful observations

- `hermes doctor` returns exit code 0 and reports version files consistent at **0.21.1**, configuration version **v41**, a valid SSL CA bundle and Codex auth logged in. These checks do not guarantee inference availability.
- Doctor reports one **running gateway profile on `gpt-5.6-sol-900k`**. Actual simultaneous provider calls are not proven. This is a reason to inspect effective background routes, not a demonstrated cause of the incident.
- `session_search` returns `success: true`, zero results and `sessions_searched: 0`; subsequent direct SQLite reads find the session. Whether discovery scope, indexing or another mechanism explains this needs a separate focused check. It is not the confirmed Codex overload.

## What help is needed

Reliable long-running agent work is still disrupted. Please help identify a supported diagnostic and configuration path on the existing Full subscription, including all active/background/auxiliary routes, and how Hermes should preserve and resume the pending model step after bounded retries fail.

The later continuation failure is now [recorded separately](ASTRA-2026-09-10.md). The useful next capture is the last completed tool result, pending step and effective request settings for that timestamp/request ID. The current evidence does **not** establish Medium as a durable fix.

## Коротко по-русски

Файл подтверждает реальный короткий проход: **Astra 900k / Medium ответила и завершила пять вызовов инструментов с финальным ответом за 51,33 секунды**. Настройка Medium найдена в сохранённой конфигурации, а не только в словах модели. Это отдельный успешный эпизод после подтверждённого отказа 5.5.

Непрерывная работа Джарвиса пока не подтверждена. Часть ранней медлительности пришлась на `hermes doctor` — около 32,5 секунды. Фоновый профиль при этом оставался на Sol 900k, но наличие реальных параллельных запросов этим не доказано. В просьбе к Hermes нужно говорить о практической непригодности нестабильного доступа для длительной работы, сохраняя этот успешный короткий эпизод в доказательствах.
